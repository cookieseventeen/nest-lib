# 影片播放清單 API 完整測試指南

## 概述

這份完整的 Postman 測試集合包含了 NestJS 影片下載與播放清單管理 API 的所有功能測試。

## 檔案說明

- `complete_video_playlist_api.postman_collection.json` - 完整測試集合
- `complete_video_playlist_api.postman_environment.json` - 環境變數配置

## 導入 Postman

1. 開啟 Postman 應用程式
2. 點擊 "Import" 按鈕
3. 選擇 `complete_video_playlist_api.postman_collection.json` 檔案
4. 再次導入 `complete_video_playlist_api.postman_environment.json` 環境檔案
5. 在右上角選擇 "影片播放清單 API 測試環境"

## 測試順序與說明

### 1. 認證管理 (必須先執行)

#### 1.1 註冊新用戶
- **路徑**: `POST /auth/register`
- **說明**: 建立測試用戶帳號
- **預期結果**: 成功建立用戶

#### 1.2 用戶登入
- **路徑**: `POST /auth/login`
- **說明**: 登入並取得 JWT Token
- **自動功能**: 成功後會自動將 Token 設定到環境變數 `jwt_token`
- **預期結果**: 取得有效的 JWT Token

#### 1.3 驗證 Token
- **路徑**: `GET /auth/profile`
- **說明**: 驗證 JWT Token 是否有效
- **預期結果**: 返回用戶資訊

### 2. 播放清單管理 (Playlist Controller)

#### 2.1 下載 YouTube 播放清單
- **路徑**: `POST /playlists/download`
- **功能**: 下載完整播放清單
- **測試 URL**: 使用環境變數中的 `test_playlist_url`
- **自動功能**: 成功後會自動設定 `playlist_id` 環境變數
- **預期結果**: 建立播放清單並下載所有影片

#### 2.2 獲取所有播放清單
- **路徑**: `GET /playlists`
- **功能**: 列出用戶所有播放清單
- **預期結果**: 返回播放清單陣列

#### 2.3 獲取特定播放清單詳情
- **路徑**: `GET /playlists/:id`
- **功能**: 取得特定播放清單的詳細資訊和影片列表
- **預期結果**: 返回播放清單詳情和包含的影片

### 3. 影片管理 (Video Controller)

#### 3.1 自動檢測下載 (單一影片)
- **路徑**: `POST /videos/download-auto`
- **功能**: 自動檢測 URL 類型並下載單一影片
- **測試 URL**: 使用環境變數中的 `test_single_video_url`
- **自動功能**: 成功後會自動設定 `video_id` 環境變數
- **預期結果**: 下載單一影片

#### 3.2 自動檢測下載 (播放清單)
- **路徑**: `POST /videos/download-auto`
- **功能**: 自動檢測播放清單 URL 並下載
- **測試 URL**: 使用環境變數中的 `test_playlist_url_2`
- **預期結果**: 建立播放清單並下載所有影片

#### 3.3 傳統單一影片下載
- **路徑**: `POST /videos/download`
- **功能**: 直接下載單一影片 (舊版 API)
- **預期結果**: 下載單一影片

#### 3.4 播放清單下載 (透過 Video Controller)
- **路徑**: `POST /videos/download-playlist`
- **功能**: 透過 Video Controller 下載播放清單
- **預期結果**: 建立播放清單並下載所有影片

#### 3.5 獲取所有影片
- **路徑**: `GET /videos`
- **功能**: 列出用戶所有影片
- **預期結果**: 返回影片陣列

#### 3.6 獲取特定影片
- **路徑**: `GET /videos/:id`
- **功能**: 取得特定影片的詳細資訊
- **預期結果**: 返回影片詳情

#### 3.7 刪除影片
- **路徑**: `DELETE /videos/:id`
- **功能**: 刪除指定影片及其檔案
- **預期結果**: 成功刪除影片

#### 3.8 透過 Video Controller 獲取播放清單
- **路徑**: `GET /videos/playlists`
- **功能**: 透過影片控制器取得播放清單列表
- **預期結果**: 返回播放清單陣列

#### 3.9 透過 Video Controller 獲取播放清單詳情
- **路徑**: `GET /videos/playlists/:id`
- **功能**: 透過影片控制器取得播放清單詳情
- **預期結果**: 返回播放清單詳情

### 4. 簡易影片 API

#### 4.1 簡易影片下載
- **路徑**: `POST /simple-video/download`
- **功能**: 不需認證的簡易影片下載
- **預期結果**: 下載影片到 uploads/simple-videos/

#### 4.2 獲取簡易影片列表
- **路徑**: `GET /simple-video`
- **功能**: 列出所有簡易下載的影片
- **預期結果**: 返回影片檔案列表

### 5. 應用程式狀態

#### 5.1 健康檢查
- **路徑**: `GET /`
- **功能**: 檢查應用程式是否正常運行
- **預期結果**: 返回 "Hello World!" 或應用程式狀態

#### 5.2 獲取應用程式資訊
- **路徑**: `GET /app`
- **功能**: 取得應用程式基本資訊
- **預期結果**: 返回應用程式版本和狀態

### 6. 錯誤處理測試

#### 6.1 無效 URL 測試
- **功能**: 測試系統對無效 URL 的錯誤處理
- **預期結果**: 返回適當的錯誤訊息

#### 6.2 未授權存取測試
- **功能**: 測試未提供認證 Token 時的存取控制
- **預期結果**: 返回 401 Unauthorized

#### 6.3 不存在的播放清單測試
- **功能**: 測試存取不存在資源的錯誤處理
- **預期結果**: 返回 404 Not Found

### 7. 檔案下載測試

#### 7.1 下載影片檔案
- **路徑**: `GET /videos/:id/download`
- **功能**: 直接下載影片檔案
- **預期結果**: 返回影片檔案

#### 7.2 串流影片檔案
- **路徑**: `GET /videos/:id/stream`
- **功能**: 串流播放影片檔案
- **預期結果**: 支援 HTTP Range 請求的影片串流

## 自動化功能

### 環境變數自動設定
- `jwt_token`: 登入成功後自動設定
- `playlist_id`: 建立播放清單成功後自動設定
- `video_id`: 下載影片成功後自動設定
- `timestamp`: 每次請求前自動產生

### 全域測試腳本
- 響應時間檢查 (< 30 秒)
- Content-Type 標頭驗證
- 狀態碼驗證

## 測試建議

### 完整測試流程
1. 先執行 "認證管理" 資料夾中的所有請求
2. 執行 "播放清單管理" 測試播放清單功能
3. 執行 "影片管理" 測試各種影片下載方式
4. 執行 "錯誤處理測試" 驗證系統穩定性
5. 最後測試檔案下載功能

### 注意事項
- 確保應用程式在 `localhost:3000` 運行
- 某些測試需要網路連線以下載 YouTube 影片
- 播放清單下載可能需要較長時間
- 建議逐個執行測試以觀察結果

### 測試資料清理
- 定期清理 `uploads/videos/` 和 `uploads/simple-videos/` 目錄
- 清理測試產生的資料庫記錄

## 預期測試結果

### 成功案例
- 所有認證相關請求應返回 200 或 201 狀態碼
- 播放清單下載應建立播放清單記錄和多個影片記錄
- 影片下載應在指定目錄建立影片檔案
- 列表查詢應返回正確的資料結構

### 錯誤案例
- 無效 URL 應返回 400 Bad Request
- 未授權請求應返回 401 Unauthorized
- 不存在資源應返回 404 Not Found

## 擴展測試

可以根據需要添加更多測試案例：
- 不同格式的影片下載 (mp3, webm)
- 大型播放清單的處理
- 並發下載測試
- 檔案大小限制測試
- 網路中斷恢復測試

## 故障排除

### 常見問題
1. **Token 過期**: 重新執行登入請求
2. **下載失敗**: 檢查 yt-dlp 是否正確安裝
3. **檔案不存在**: 確認檔案路徑和權限設定
4. **連線問題**: 檢查網路連線和防火牆設定

### 日誌檢查
- 應用程式控制台輸出
- Postman Console 日誌
- 系統檔案權限檢查
